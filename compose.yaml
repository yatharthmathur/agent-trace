services:
  check:
    build: .
    working_dir: /app
    volumes:
      - .:/app
      - /app/.venv
    command:
      - /bin/sh
      - -c
      - uv sync --locked --all-groups && uv run ruff check . && uv run ruff format --check . && uv run ty check src tests
